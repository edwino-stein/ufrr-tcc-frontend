import { VAlignDirective } from './v-align.directive';

describe('VAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new VAlignDirective();
    expect(directive).toBeTruthy();
  });
});
